You can use this sentence in proposals:

I also have a portfolio project where I built a Python API data sync tool that fetches data from a REST API, cleans and validates records, removes duplicates, stores the data in SQLite, and exports the results to CSV/Excel.